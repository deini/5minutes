---
# Please change these variables as you need, especially password
server_user_name: deini

# pip install passlib
# python -c "from passlib.hash import sha512_crypt; import getpass; print sha512_crypt.encrypt(getpass.getpass())"
server_user_password: __CHANGE_ME__
logwatch_email: danielalmaguer@gmail.com

# Glob list for ssh keys
user_public_keys:
  - ~/.ssh/id_rsa.pub
#  - ~/dev/deini/ssh-authorized-keys/*.pub

required_packages:
  - ufw
  - fail2ban
  - unattended-upgrades
  - logwatch

ufw_allowed_ports:
  - 22
  - 80
  - 443

timezone: America/Chicago
